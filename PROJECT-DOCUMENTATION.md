# AI工具大全 - 项目文档

> **文档版本**: 1.0
> **最后更新**: 2026-01-08
> **网站地址**: https://aidaquanji.com
> **服务器**: 阿里云 ECS (8.130.157.251)

---

## 📋 目录

1. [项目概述](#项目概述)
2. [技术架构](#技术架构)
3. [部署环境](#部署环境)
4. [核心功能](#核心功能)
5. [数据库结构](#数据库结构)
6. [重要文件说明](#重要文件说明)
7. [部署流程](#部署流程)
8. [问题与解决方案](#问题与解决方案)
9. [运维命令](#运维命令)
10. [待讨论事项](#待讨论事项)
11. [竞品分析](#竞品分析)
12. [未来规划](#未来规划)
13. [变更日志](#变更日志)
14. [相关链接](#相关链接)
15. [联系方式](#联系方式)

---

## 📖 项目概述

### 项目目标

创建一个 AI 工具导航网站，为用户提供：
- 分类展示各类 AI 工具
- 智能排名和推荐
- 工具对比功能
- 使用案例展示
- 定期更新最新的 AI 工具

### 核心价值

- 🎯 **精选优质工具**: 汇集全球优秀 AI 工具
- 📊 **智能排名**: 基于多维度评分系统
- 🔄 **定期更新**: 每周自动/半自动更新工具库
- 🇨🇳 **国内友好**: 适配国内访问环境

---

## 🏗️ 技术架构

### 前端技术栈

- **框架**: Next.js 15.5.9 (App Router)
- **UI 组件**: Ant Design
- **样式**: Tailwind CSS
- **语言**: TypeScript

### 后端技术栈

- **运行时**: Node.js 20
- **ORM**: Prisma 5.22.0
- **数据库**: MySQL 8.0
- **认证**: NextAuth.js

### 基础设施

- **容器化**: Docker + Docker Compose
- **Web 服务器**: Nginx (反向代理 + SSL)
- **部署**: 阿里云 ECS
- **SSL 证书**: Let's Encrypt (有效期至 2026-03-31)

### 架构图

```
用户请求 (HTTPS)
    ↓
Nginx (443) → SSL 终止
    ↓
Next.js 应用 (3000) → Server Components + API Routes
    ↓
Prisma ORM
    ↓
MySQL 数据库 (3306)
```

---

## 🌐 部署环境

### 服务器信息

- **云服务商**: 阿里云 (Aliyun)
- **服务器 IP**: 8.130.157.251
- **域名**: aidaquanji.com
- **操作系统**: Linux (Docker 环境)
- **内存**: ~2GB RAM + 8GB Swap

### Docker 容器

| 容器名 | 镜像 | 端口映射 | 状态 |
|--------|------|----------|------|
| aitools-mysql | mysql:8.0 | 3306 (内部) | 健康运行 |
| aitools-web | aitools-web (自建) | 3000 (内部) | 运行中 |
| aitools-nginx | nginx:alpine | 80, 443 → 外部 | 运行中 |

### 目录结构

```
/opt/aitools/                    # 项目根目录
├── .env                         # 环境变量（已加入 .gitignore）
├── .env.example                 # 环境变量示例
├── docker-compose.prod.yml      # 生产环境 Docker 配置
├── Dockerfile                   # Docker 构建文件
├── docker-entrypoint.sh         # 容器启动脚本
├── nginx/                       # Nginx 配置
│   └── default.conf
├── ssl/                         # SSL 证书
│   ├── fullchain.pem
│   └── privkey.pem
├── scripts/                     # 脚本目录
│   ├── deploy.sh               # 部署脚本
│   ├── add-sample-data.ts      # 示例数据导入
│   └── ...
├── src/                         # 源代码
│   ├── app/                    # Next.js App Router
│   ├── components/             # React 组件
│   ├── lib/                    # 工具库
│   └── ...
├── prisma/                      # Prisma 配置
│   ├── schema.prisma           # 数据库模型
│   ├── seed.ts                 # 种子数据
│   └── migrations/             # 数据库迁移
└── backups/                     # 备份目录
    ├── .env.latest             # 最新环境变量备份
    └── .env.backup-*           # 历史备份
```

---

## ✨ 核心功能

### 已实现功能

#### 1. 首页功能
- ✅ 动态展示 6 个分类
- ✅ 实时显示每个分类的工具数量（从数据库查询）
- ✅ 精选工具展示（按评分排序，Top 4）
- ✅ 响应式设计

**实现方式**:
- Server Component 从数据库实时查询数据
- Client Component 处理 UI 渲染（Ant Design 组件）
- 强制动态渲染 (`export const dynamic = 'force-dynamic'`)

**关键文件**:
- `src/app/page.tsx` - Server Component，数据查询
- `src/app/HomeContent.tsx` - Client Component，UI 渲染

#### 2. 分类页面
- ✅ 展示指定分类下的所有工具
- ✅ 工具卡片展示（名称、描述、logo、价格类型等）
- ✅ 动态路由 (`/category/[slug]`)

**关键文件**:
- `src/app/category/[slug]/page.tsx`
- `src/app/category/[slug]/CategoryContent.tsx`

#### 3. 工具详情页
- ✅ 完整的工具信息展示
- ✅ 评论功能
- ✅ 收藏功能
- ✅ 对比功能

**关键文件**:
- `src/app/tool/[slug]/page.tsx`
- `src/app/tool/[slug]/ToolContent.tsx`

#### 4. 搜索功能
- ✅ 关键词搜索
- ✅ 多维度筛选（分类、价格、团队来源、平台）
- ✅ 排序功能

**关键文件**:
- `src/app/search/page.tsx`
- `src/app/api/search/route.ts`

#### 5. 工具对比
- ✅ 多工具并排对比
- ✅ 详细参数对比

**关键文件**:
- `src/app/compare/page.tsx`

### 数据爬虫功能

#### 已实现的爬虫
1. **WaytoAGI 爬虫** (`src/lib/scrapers/WaytoAGIScraper.ts`)
   - 使用 Puppeteer 爬取飞书文档
   - 支持手动登录
   - 提取工具名称、分类、描述、URL

2. **GitHub Stars 爬虫** (`src/lib/scrapers/GitHubScraper.ts`)
   - 获取 GitHub 项目星标数

3. **Product Hunt 爬虫** (`src/lib/scrapers/ProductHuntScraper.ts`)
   - 获取 Product Hunt 投票数

---

## 🗄️ 数据库结构

### 核心数据表

#### categories (分类表)
```sql
id          VARCHAR(191) PRIMARY KEY
name        VARCHAR(191) UNIQUE      -- 分类名称（如：语言、画图）
slug        VARCHAR(191) UNIQUE      -- URL slug（如：language, image）
description TEXT                     -- 分类描述
icon        VARCHAR(191)             -- 图标 emoji
order       INT                      -- 排序顺序
createdAt   DATETIME
updatedAt   DATETIME
```

**当前数据**（6个分类）:
| name | slug | icon | 工具数 |
|------|------|------|--------|
| 语言 | language | 💬 | 4 |
| 画图 | image | 🎨 | 3 |
| 编程 | code | 💻 | 3 |
| 视频 | video | 🎬 | 2 |
| 笔记 | note | 📝 | 2 |
| 个人助理 | assistant | 🤖 | 2 |

#### tools (工具表)
```sql
id                    VARCHAR(191) PRIMARY KEY
name                  VARCHAR(191)             -- 工具名称
slug                  VARCHAR(191) UNIQUE      -- URL slug
description           TEXT                     -- 简短描述
fullDescription       LONGTEXT                 -- 完整描述
logoUrl               VARCHAR(191)             -- Logo URL
websiteUrl            VARCHAR(191)             -- 官网 URL
teamOrigin           ENUM('DOMESTIC','OUTBOUND','OVERSEAS')  -- 团队来源
platformAvailability  JSON                     -- 平台支持
pricingType          ENUM('FREE','PAID','FREEMIUM')  -- 价格类型
pricingDetails       TEXT                     -- 价格详情
rankingScore         DOUBLE                   -- 排名分数
isPublished          BOOLEAN                  -- 是否发布
isFeatured           BOOLEAN                  -- 是否精选
dataSource           VARCHAR(191)             -- 数据来源
categoryId           VARCHAR(191)             -- 分类 ID (外键)
createdAt            DATETIME
updatedAt            DATETIME
```

**当前数据**: 16 个工具

**示例工具**:
- ChatGPT (rankingScore: 95)
- Midjourney (rankingScore: 92)
- GitHub Copilot (rankingScore: 90)
- Claude (rankingScore: 90)
- 等...

#### 其他表
- `users` - 用户表
- `accounts` - 账号关联表 (NextAuth)
- `sessions` - 会话表 (NextAuth)
- `admins` - 管理员表
- `comments` - 评论表
- `favorites` - 收藏表
- `use_cases` - 使用案例表
- `ranking_metrics` - 排名指标表

---

## 📄 重要文件说明

### 配置文件

#### `.env` - 环境变量（⚠️ 重要）
```bash
# MySQL 配置
MYSQL_ROOT_PASSWORD=<your-root-password>
MYSQL_DATABASE=aitools
MYSQL_USER=aitools_user
MYSQL_PASSWORD=<your-mysql-password>

# 数据库连接 URL（⚠️ 密码需要 URL 编码）
DATABASE_URL="mysql://aitools_user:<url-encoded-password>@mysql:3306/aitools"
# 注意: ! → %21, @ → %40, # → %23

# NextAuth 配置
NEXTAUTH_URL="https://aidaquanji.com"
NEXTAUTH_SECRET="<your-nextauth-secret-here>"

# GitHub Token
GITHUB_TOKEN="ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# 管理员邮箱
ADMIN_EMAIL="davidliuzhibo@foxmail.com"

# Cron 密钥
CRON_SECRET="<your-cron-secret-here>"
```

**⚠️ 关键注意事项**:
1. **DATABASE_URL 中的密码必须进行 URL 编码**
   - 原密码示例: `MyPass2024!@#`
   - 编码后示例: `MyPass2024%21%40%23`
   - 如果不编码,Prisma 会报错：`empty host in database URL`

2. **备份位置**: `/opt/aitools/backups/.env.latest`

3. **已加入 .gitignore**: 避免 git pull 时被覆盖

#### `next.config.ts` - Next.js 配置
```typescript
{
  reactStrictMode: true,
  output: 'standalone',  // ⚠️ Docker 部署必需
  images: {
    remotePatterns: [{ protocol: 'https', hostname: '**' }]
  },
  experimental: {
    serverActions: { bodySizeLimit: '2mb' }
  }
}
```

#### `docker-compose.prod.yml` - Docker 编排配置
定义了 3 个服务：
- `mysql`: MySQL 8.0 数据库
- `web`: Next.js 应用
- `nginx`: Nginx 反向代理

#### `Dockerfile` - Docker 构建文件
多阶段构建：
1. **base**: Node.js 基础镜像
2. **deps**: 安装依赖
3. **builder**: 构建应用
4. **runner**: 运行时镜像

**关键配置**:
```dockerfile
# Prisma CLI 全局安装（解决迁移问题）
RUN npm install -g prisma@5.22.0

# 用户权限配置
RUN adduser --system --uid 1001 --home /app nextjs
RUN mkdir -p /app/.npm && chown -R nextjs:nodejs /app
```

#### `docker-entrypoint.sh` - 容器启动脚本
```bash
#!/bin/sh
set -e
echo "等待数据库连接..."
prisma migrate deploy  # 运行数据库迁移
exec "$@"              # 启动 Next.js
```

### 脚本文件

#### `scripts/deploy.sh` - 部署脚本
一键部署脚本，执行以下步骤：
1. 检查必要软件 (Docker, Docker Compose)
2. 检查环境变量
3. 检查 SSL 证书
4. 停止旧容器
5. 构建 Docker 镜像
6. 启动容器
7. 运行数据库迁移
8. 检查服务状态

#### `prisma/seed.ts` - 数据库种子数据
创建 6 个基础分类。

#### `scripts/add-sample-data.ts` - 示例工具数据
创建 17 个示例工具（现在数据库中只有 16 个，可能有重复 slug）。

---

## 🚀 部署流程

### 初次部署

#### 1. 服务器准备
```bash
# SSH 登录
ssh root@8.130.157.251

# 创建项目目录
mkdir -p /opt/aitools
cd /opt/aitools

# 克隆代码
git clone https://github.com/davidliuzhibo/ai-tools-directory.git .
```

#### 2. 配置环境变量
```bash
# 复制环境变量模板
cp .env.example .env

# 编辑环境变量（使用 nano）
nano .env

# ⚠️ 确保 DATABASE_URL 密码已 URL 编码
DATABASE_URL="mysql://aitools_user:<url-encoded-password>@mysql:3306/aitools"
```

#### 3. 配置 SSL 证书
```bash
# 确保 SSL 证书文件存在
ls -l ssl/fullchain.pem ssl/privkey.pem
```

#### 4. 部署
```bash
# 执行部署脚本
bash scripts/deploy.sh
```

#### 5. 初始化数据
```bash
# 创建分类
docker exec -it aitools-web npx tsx prisma/seed.ts

# 导入示例工具
docker exec -it aitools-web mkdir -p /app/scripts
docker cp scripts/add-sample-data.ts aitools-web:/app/scripts/
docker exec -it aitools-web npx tsx /app/scripts/add-sample-data.ts
```

### 日常更新部署

```bash
# 1. SSH 登录
ssh root@8.130.157.251

# 2. 进入项目目录
cd /opt/aitools

# 3. 拉取最新代码
git pull origin master

# 4. 重新部署
bash scripts/deploy.sh

# 5. 如果遇到数据库连接问题，恢复 .env
cp /opt/aitools/backups/.env.latest /opt/aitools/.env
docker compose -f docker-compose.prod.yml down web
docker compose -f docker-compose.prod.yml up -d web
```

---

## ⚠️ 问题与解决方案

### 问题 1: Prisma CLI 在 Docker standalone 模式下缺失

**错误现象**:
```
/app/docker-entrypoint.sh: node_modules/.bin/prisma: not found
```

**根本原因**:
Next.js standalone 输出模式不包含所有 node_modules，Prisma CLI 的二进制文件和 WASM 依赖缺失。

**解决方案**:
在 Dockerfile 的 runner 阶段全局安装 Prisma CLI：
```dockerfile
RUN npm install -g prisma@5.22.0
```

修改 `docker-entrypoint.sh` 使用全局 prisma：
```bash
prisma migrate deploy
```

**相关 Commit**: b9d7c9c

---

### 问题 2: 数据库连接字符串特殊字符解析错误

**错误现象**:
```
Error: P1013: The provided database string is invalid. empty host in database URL.
```

**根本原因**:
`.env` 中的密码包含特殊字符 `!@#`，其中 `@` 在 URL 中有特殊含义（用于分隔用户信息和主机），导致 Prisma 解析失败。

**解决方案**:
对 DATABASE_URL 中的特殊字符进行 URL 编码：
- `!` → `%21`
- `@` → `%40`
- `#` → `%23`

修改后的 DATABASE_URL：
```
DATABASE_URL="mysql://aitools_user:<url-encoded-password>@mysql:3306/aitools"
# 例如，如果密码是 MyPass!@#，则编码为 MyPass%21%40%23
```

**备份策略**:
```bash
# 备份正确的 .env
cp .env /opt/aitools/backups/.env.latest

# 将 .env 加入 .gitignore
echo ".env" >> .gitignore
```

**快速修复命令**:
```bash
cp /opt/aitools/backups/.env.latest /opt/aitools/.env
docker compose -f docker-compose.prod.yml down web
docker compose -f docker-compose.prod.yml up -d web
```

---

### 问题 3: docker compose restart 不重新加载环境变量

**错误现象**:
修改了 `.env` 文件后，执行 `docker compose restart web`，容器仍然使用旧的环境变量。

**根本原因**:
`docker compose restart` 只是重启容器，不会重新加载环境变量。

**解决方案**:
必须删除并重新创建容器：
```bash
docker compose -f docker-compose.prod.yml down web
docker compose -f docker-compose.prod.yml up -d web
```

---

### 问题 4: 首页构建时尝试连接数据库

**错误现象**:
```
Error occurred prerendering page "/"
Invalid `prisma.categories.findMany()` invocation
error: Error validating datasource `db`: the URL must start with the protocol `mysql://`
```

**根本原因**:
首页改为 Server Component 后，Next.js 在构建时会尝试预渲染页面，此时数据库尚未启动。

**解决方案**:
在首页添加强制动态渲染：
```typescript
// src/app/page.tsx
export const dynamic = 'force-dynamic';
```

**相关 Commit**: 7072a28

---

### 问题 5: Ant Design 组件在 Server Component 中报错

**错误现象**:
```
Error: Element type is invalid: expected a string (for built-in components)
or a class/function (for composite components) but got: undefined.
```

**根本原因**:
Ant Design 组件都是客户端组件（需要浏览器环境），不能直接在 Server Component 中使用。

**解决方案**:
创建分离的 Client Component 处理 UI：

1. **Server Component** (`src/app/page.tsx`):
   - 负责数据查询
   - 传递数据给 Client Component

2. **Client Component** (`src/app/HomeContent.tsx`):
   - 使用 `'use client'` 指令
   - 渲染 Ant Design 组件
   - 接收 props 显示数据

**相关文件**: 61ee465

---

### 问题 6: Docker 镜像拉取失败

**错误现象**:
```
failed to resolve source metadata for docker.io/library/node:20-slim:
failed to do request: dial tcp: connect: connection refused
```

**根本原因**:
Docker Hub 在国内访问不稳定，默认镜像源无法连接。

**解决方案**:
配置国内 Docker 镜像源：

```bash
# 编辑 Docker 配置
nano /etc/docker/daemon.json

# 添加镜像源
{
  "registry-mirrors": [
    "https://docker.1ms.run",
    "https://docker.rainbond.cc",
    "https://docker.fxxk.dedyn.io",
    "https://docker.chenby.cn"
  ],
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  }
}

# 重启 Docker
systemctl restart docker
```

---

### 问题 7: Docker 构建内存不足

**错误现象**:
```
failed to execute bake: signal: killed
```

**根本原因**:
服务器内存 (~2GB) 不足以支持 Docker 构建过程。

**解决方案**:
清理 Docker 缓存释放空间：
```bash
docker system prune -a -f
docker builder prune -a -f

# 可选：调整内存过载策略
echo 1 > /proc/sys/vm/overcommit_memory
```

---

### 问题 8: npm 缓存目录权限问题

**错误现象**:
```
Your cache folder contains root-owned files
EACCES: permission denied, mkdir '/app/.npm'
```

**根本原因**:
容器内的 nextjs 用户没有权限创建 npm 缓存目录。

**解决方案**:
在 Dockerfile 中创建目录并设置权限：
```dockerfile
RUN mkdir -p /app/.npm && \
    chown -R nextjs:nodejs /app
```

---

## 🛠️ 运维命令

### Docker 容器管理

```bash
# 查看所有容器状态
docker compose -f docker-compose.prod.yml ps

# 查看容器日志
docker compose -f docker-compose.prod.yml logs -f web
docker compose -f docker-compose.prod.yml logs --tail=50 web

# 重启容器
docker compose -f docker-compose.prod.yml restart web

# 停止所有容器
docker compose -f docker-compose.prod.yml down

# 启动所有容器
docker compose -f docker-compose.prod.yml up -d

# 重建并启动容器
docker compose -f docker-compose.prod.yml up -d --build

# 进入容器
docker exec -it aitools-web sh
docker exec -it aitools-mysql bash
```

### 数据库管理

```bash
# 连接 MySQL（使用实际密码）
docker exec -it aitools-mysql mysql -u aitools_user -p aitools
# 输入密码后按回车

# 常用 SQL 查询
SELECT COUNT(*) FROM categories;
SELECT COUNT(*) FROM tools;
SELECT name, slug FROM categories ORDER BY `order`;
SELECT name, rankingScore FROM tools ORDER BY rankingScore DESC LIMIT 10;

# 数据库备份（使用实际密码）
docker exec aitools-mysql mysqldump -u aitools_user -p aitools > backup.sql

# 数据库恢复（使用实际密码）
cat backup.sql | docker exec -i aitools-mysql mysql -u aitools_user -p aitools
```

### Prisma 操作

```bash
# 在容器内运行 Prisma 命令
docker exec -it aitools-web prisma migrate status
docker exec -it aitools-web prisma migrate deploy
docker exec -it aitools-web prisma db push
docker exec -it aitools-web prisma studio  # 打开 Prisma Studio GUI
```

### 数据导入

```bash
# 导入分类数据
docker exec -it aitools-web npx tsx prisma/seed.ts

# 导入工具数据
docker exec -it aitools-web npx tsx /app/scripts/add-sample-data.ts
```

### 日志查看

```bash
# 查看 Nginx 日志
docker exec aitools-nginx cat /var/log/nginx/access.log
docker exec aitools-nginx cat /var/log/nginx/error.log

# 查看 Docker 日志
journalctl -u docker -f

# 查看系统资源
docker stats
free -h
df -h
```

### SSL 证书管理

```bash
# 检查证书有效期
openssl x509 -in ssl/fullchain.pem -noout -dates

# 更新证书（假设使用 Certbot）
certbot renew
cp /etc/letsencrypt/live/aidaquanji.com/fullchain.pem ssl/
cp /etc/letsencrypt/live/aidaquanji.com/privkey.pem ssl/
docker compose -f docker-compose.prod.yml restart nginx
```

### 清理与维护

```bash
# 清理 Docker 缓存
docker system prune -f
docker image prune -f
docker volume prune -f

# 清理构建缓存
docker builder prune -f

# 查看磁盘使用
docker system df
```

---

## ❓ 待讨论事项

### 半自动化工具更新系统 - 详细方案讨论

> **背景**: 已确定使用方案 2（半自动化更新，不需人工审核）
> **目标**: 每周自动更新工具数据
> **状态**: 待讨论以下细节后实施

---

#### 问题 1: 去重策略

当抓取到的工具已存在于数据库时，应该如何处理？

**选项 A: 完全跳过**
- 保持现有数据不变
- 不更新任何字段
- ✅ **优点**: 最安全，不会破坏现有数据
- ❌ **缺点**: 无法更新工具的新信息（如评分、描述变化）

**选项 B: 更新部分字段**
- 更新动态字段：rankingScore、description、logoUrl
- 保留核心字段：name、slug、categoryId
- ✅ **优点**: 既能更新信息，又保护核心数据
- ⚠️ **注意**: 需要明确定义哪些字段可更新

**选项 C: 完全覆盖**
- 使用最新抓取的数据替换所有字段
- ❌ **缺点**: 可能破坏人工修正的数据

**待决策**: 选择哪个选项？

---

#### 问题 2: 判断"重复"的依据

如何判断两个工具是同一个？

**选项 A: 通过 `websiteUrl`**
- 比较官网 URL 是否相同
- ✅ **优点**: 最准确，URL 是唯一标识
- ❌ **缺点**: 如果官网 URL 变化，会被当作新工具

**选项 B: 通过 `name`**
- 比较工具名称是否相同
- ❌ **缺点**: 可能误判同名但不同的工具（如：多个叫 "AI Assistant" 的工具）

**选项 C: 通过 `slug`**
- 比较 URL slug 是否相同
- ⚠️ **注意**: 需要标准化 slug 生成规则

**选项 D: 综合判断（推荐）**
- 优先比对 `websiteUrl`
- 如果 URL 不同但 `name` 相似度很高（如 > 90%），人工审核或记录日志
- 使用模糊匹配算法

**待决策**: 选择哪个选项？推荐选项 D 或 A

---

#### 问题 3: 新分类的处理

如果爬虫抓到一个不在现有 6 个分类中的工具，应该怎么办？

**当前分类**: 语言、画图、编程、视频、笔记、个人助理

**选项 A: 自动创建新分类**
- 根据抓取的分类名自动创建
- ❌ **缺点**: 可能导致分类混乱、名称不规范

**选项 B: 映射到最接近的现有分类（推荐）**
- 建立分类映射表
- 例如：
  ```
  "AI写作" → "语言"
  "图片编辑" → "画图"
  "代码助手" → "编程"
  "音频处理" → 创建新的"音频"分类或映射到"视频"
  ```
- ✅ **优点**: 可控，保持分类体系清晰

**选项 C: 放入"其他"或"未分类"**
- 创建一个通用的"其他"分类
- 定期人工审核并重新分类
- ⚠️ **注意**: 需要定期维护

**选项 D: 跳过该工具**
- 不导入无法匹配分类的工具
- ❌ **缺点**: 可能漏掉优质工具

**待决策**: 选择哪个选项？推荐选项 B（需要建立映射表）

**分类映射表示例**:
```typescript
const categoryMapping = {
  // 爬虫分类 → 数据库分类
  'AI写作': 'language',
  'AI对话': 'language',
  'ChatGPT': 'language',
  '图片生成': 'image',
  '图像编辑': 'image',
  'AI绘画': 'image',
  '代码助手': 'code',
  '编程工具': 'code',
  'GitHub工具': 'code',
  '视频生成': 'video',
  '视频编辑': 'video',
  // ... 更多映射
};
```

---

#### 问题 4: 定时任务触发方式

每周如何自动运行更新？

**选项 A: 服务器 cron job**
```bash
# 每周日凌晨 2 点执行
0 2 * * 0 cd /opt/aitools && node scripts/update-tools.js >> logs/update.log 2>&1
```
- ✅ **优点**: 简单直接，服务器本地执行
- ⚠️ **注意**: 需要确保脚本执行环境正确

**选项 B: GitHub Actions**
- 在 GitHub 上设置定时工作流
- 自动执行爬虫 → 生成数据 → 推送到服务器
- ✅ **优点**: 不依赖服务器资源，易于监控
- ❌ **缺点**: 需要配置服务器访问权限

**选项 C: 手动运行命令（推荐初期）**
```bash
# 每周手动执行
ssh root@8.130.157.251
cd /opt/aitools
node scripts/update-tools.js
```
- ✅ **优点**: 完全可控，先测试稳定性
- ❌ **缺点**: 需要记得定期执行

**待决策**: 选择哪个选项？推荐初期用 C，稳定后改为 A

---

#### 问题 5: 数据质量保护措施

为了防止爬虫失败导致数据库被破坏：

**保护措施清单**:

1. **数据量检查**
   - 如果抓取的工具数量 < 10，认为异常，不执行导入
   - 如果新增工具数量异常多（如 > 100），发出警告

2. **自动备份**
   - 导入前自动备份数据库
   - 保留最近 7 天的备份
   ```bash
   docker exec aitools-mysql mysqldump -u aitools_user -p aitools > \
     backups/db-backup-$(date +%Y%m%d).sql
   # 运行时需要输入密码
   ```

3. **异常检测**
   - 检查必填字段是否为空
   - 检查 URL 格式是否正确
   - 检查分类是否有效

4. **日志记录**
   - 记录每次更新的详细日志
   - 包括：新增数量、跳过数量、失败数量、错误信息
   - 日志文件位置：`/opt/aitools/logs/update-YYYYMMDD.log`

5. **回滚机制**
   - 如果导入后发现问题，能够快速回滚到备份

**待决策**: 是否需要所有这些保护措施？（推荐：全部启用）

---

#### 问题 6: 更新通知

更新完成后，是否需要通知？如何通知？

**选项 A: 邮件通知**
```
收件人: davidliuzhibo@foxmail.com
主题: AI工具大全 - 每周更新报告
内容:
  - 更新时间: 2026-01-15 02:00
  - 新增工具: 15 个
  - 更新工具: 8 个
  - 跳过工具: 23 个
  - 失败数量: 0 个
  - 详细日志: /opt/aitools/logs/update-20260115.log
```
- ✅ **优点**: 及时知道更新情况
- ⚠️ **注意**: 需要配置邮件发送服务

**选项 B: 仅生成日志文件**
- 保存到 `/opt/aitools/logs/update-YYYYMMDD.log`
- 需要时手动查看
- ✅ **优点**: 简单，无需额外配置

**选项 C: 推送到 Webhook/钉钉/飞书**
- 更现代的通知方式
- ⚠️ **注意**: 需要配置第三方服务

**选项 D: 不需要通知**
- 信任自动化流程
- ❌ **缺点**: 无法及时发现问题

**待决策**: 选择哪个选项？推荐至少保留日志文件（选项 B）

---

#### 问题 7: 数据源优先级与合并策略

现有数据源:
1. WaytoAGI（飞书文档）
2. Product Hunt
3. GitHub Stars

**场景**: 同一个工具在多个数据源都有，应该如何处理？

**选项 A: 只使用第一个数据源**
- 按优先级顺序：WaytoAGI > Product Hunt > GitHub
- 如果在 WaytoAGI 找到，就不再从其他源获取
- ✅ **优点**: 简单，避免数据冲突
- ❌ **缺点**: 可能错过更好的数据

**选项 B: 合并多个数据源的信息**
- 基本信息（name, url, description）：使用第一个数据源
- 评分（rankingScore）：综合多个来源计算
  ```
  rankingScore = (waytoagi_score * 0.5) + (producthunt_votes * 0.3) + (github_stars * 0.2)
  ```
- ✅ **优点**: 信息更全面，评分更准确
- ⚠️ **复杂度**: 需要数据归一化和权重计算

**选项 C: 分别导入，后期去重**
- 先从所有数据源导入
- 导入后运行去重脚本
- ⚠️ **注意**: 可能产生重复数据

**待决策**: 选择哪个选项？推荐选项 B（更准确）或选项 A（更简单）

---

### 决策总结表

| 问题 | 推荐方案 | 状态 |
|-----|---------|------|
| 1. 去重策略 | 选项 A 或 B | ⏳ 待决策 |
| 2. 重复判断依据 | 选项 D (综合) 或 A (URL) | ⏳ 待决策 |
| 3. 新分类处理 | 选项 B (映射表) | ⏳ 待决策，需建立映射 |
| 4. 触发方式 | 选项 C → A (手动测试后自动化) | ⏳ 待决策 |
| 5. 数据保护 | 全部启用 | ⏳ 待决策 |
| 6. 更新通知 | 选项 B (日志) | ⏳ 待决策 |
| 7. 数据源优先级 | 选项 B (合并) 或 A (单源) | ⏳ 待决策 |

---

## 📊 竞品分析

### AICPB - AI产品榜

> **网站地址**: https://www.aicpb.com/zh
> **最后分析时间**: 2026-01-08

#### 产品概述

AICPB（AI产品榜）是国内最权威的AI产品排行榜之一，提供全面的AI产品信息和数据驱动的排名系统。

#### 核心特点

1. **规模优势**
   - 收录 10,000+ AI产品
   - 覆盖 100+ 细分类别
   - 每日更新产品动态

2. **数据驱动的排名系统**
   - **AI新榜指数**: 综合评分系统
   - 多维度评估：技术能力、用户口碑、市场表现、创新程度
   - 实时数据更新和趋势追踪

3. **丰富的内容维度**
   - 产品详细介绍和使用指南
   - 用户评价和使用案例
   - 行业动态和趋势分析
   - 产品对比功能

4. **专业的评测体系**
   - 专业编辑团队评测
   - 用户真实反馈
   - 数据可视化展示
   - 定期发布行业报告

5. **社区互动**
   - 用户评论和讨论
   - 产品收藏和分享
   - 使用经验交流

#### 与我们项目的对比

| 维度 | AICPB | 我们的项目 |
|------|-------|-----------|
| 产品数量 | 10,000+ | 16 |
| 分类数量 | 100+ | 6 |
| 更新频率 | 每日 | 计划每周 |
| 评分系统 | AI新榜指数（多维度） | rankingScore（单一分数） |
| 用户功能 | 完善（评论、收藏、讨论） | 基础（部分已实现但未启用） |
| 内容深度 | 深度评测 + 使用指南 | 基础信息展示 |
| 数据来源 | 多源 + 专业编辑 | 计划：爬虫 + 半自动 |

#### 可借鉴的优势

##### 短期可实现（1-2个月）

1. **扩展分类体系**
   - 从 6 个分类扩展到 20-30 个细分类别
   - 建立分类层级结构（一级分类 → 二级分类）
   - 参考 AICPB 的分类方式

2. **增强数据维度**
   - 添加产品标签系统（如：免费、开源、企业级）
   - 记录产品更新日期和版本信息
   - 添加产品热度指标（浏览量、收藏量）

3. **优化排名算法**
   - 从单一 rankingScore 改为多维度评分
   - 考虑因素：GitHub Stars、Product Hunt 投票、用户评分、更新频率
   - 实现加权计算和动态调整

4. **改进首页展示**
   - 添加"热门上升榜"（最近热度增长最快的工具）
   - 添加"新品推荐"（最近加入的新工具）
   - 添加"编辑推荐"标签

##### 中期可实现（3-6个月）

1. **完善自动化更新系统**
   - 实现每日或每周自动抓取
   - 建立数据质量检查机制
   - 实现产品信息自动补全（logo、描述、标签等）

2. **用户参与功能**
   - 开启用户评论系统
   - 实现用户评分（五星制）
   - 用户可以提交新产品
   - 建立用户贡献激励机制

3. **内容深度提升**
   - 添加使用案例（Use Cases）
   - 编写工具使用教程
   - 产品更新日志追踪
   - 工具对比专题文章

4. **数据可视化**
   - 产品热度趋势图
   - 分类占比统计
   - 地域分布（国内/国外团队）
   - 价格分布分析

##### 长期目标（6-12个月）

1. **建立权威评测体系**
   - 设计"AI工具新榜指数"或类似评分系统
   - 多维度评估：功能性、易用性、创新性、性价比、稳定性
   - 定期发布行业报告和趋势分析
   - 建立专家评测团队

2. **深化内容运营**
   - 制作工具测评视频
   - 邀请行业专家撰写深度分析
   - 举办 AI 工具评选活动
   - 建立工具开发者合作计划

3. **社区建设**
   - 建立问答社区（类似知乎）
   - 用户经验分享平台
   - 工具使用技巧交流
   - 行业从业者社群

4. **商业化探索**
   - 工具推广服务
   - 付费会员（高级功能、数据报告）
   - 广告展示（精准投放）
   - 企业定制方案

#### 差异化策略

虽然 AICPB 规模更大、更权威，但我们可以通过以下策略建立差异化优势：

1. **垂直深耕**
   - 选择 2-3 个细分领域深度运营
   - 成为特定领域的专家平台
   - 例如：专注于"开发者AI工具"或"内容创作AI工具"

2. **技术创新**
   - 使用 AI 自动生成工具描述和使用建议
   - 智能推荐算法（基于用户需求匹配工具）
   - 自动化工具测试和性能评估

3. **社区氛围**
   - 打造更友好、活跃的社区
   - 鼓励用户生成内容（UGC）
   - 建立用户认证体系（如：认证专家、优质贡献者）

4. **国际化**
   - 提供中英双语支持
   - 覆盖国际主流 AI 工具
   - 连接国内外 AI 社区

#### 待讨论问题

1. **发展方向**：是否要追求 AICPB 的大而全模式，还是选择垂直细分领域？
2. **资源投入**：当前团队规模下，优先实现哪些功能？
3. **内容质量 vs 数量**：初期是优先增加工具数量，还是提升现有工具的信息质量？
4. **用户定位**：目标用户是谁？（开发者、产品经理、普通用户、企业决策者）
5. **差异化竞争**：如何在 AICPB 等成熟平台面前建立独特价值？

---

## 🔮 未来规划

### 短期计划（已讨论，待实施）

#### 1. 半自动化工具更新系统
- ✅ 方案已确定：方案 2（半自动化更新，不需人工审核）
- ⏳ 待讨论细节：
  - 去重策略
  - 分类映射规则
  - 数据质量保护措施
  - 定时任务配置

#### 2. 多数据源集成
- WaytoAGI (已有爬虫)
- Product Hunt (已有爬虫)
- GitHub Stars (已有爬虫)
- 未来可能添加更多数据源

#### 3. 更新频率
- 目标：每周更新一次
- 方式：定时任务或手动触发

### 中期计划

#### 1. 管理后台完善
- 工具管理界面
- 分类管理界面
- 用户管理界面
- 数据统计看板

#### 2. 用户功能增强
- 用户注册/登录
- 个人收藏夹
- 工具评分系统
- 评论互动

#### 3. SEO 优化
- 站点地图生成
- 结构化数据标记
- 页面性能优化
- 移动端体验优化

### 长期愿景

#### 1. AI 推荐系统
- 基于用户行为的个性化推荐
- 智能工具对比
- 使用场景匹配

#### 2. 社区功能
- 用户评价系统
- 使用案例分享
- 工具评测文章
- 问答社区

#### 3. API 服务
- 开放 API 供第三方调用
- 数据订阅服务
- Webhook 通知

---

## 📝 变更日志

### 2026-01-08
- ✅ 完成初次部署到阿里云服务器
- ✅ 解决 Prisma CLI 在 Docker standalone 模式下的问题
- ✅ 解决数据库连接字符串特殊字符编码问题
- ✅ 实现首页动态数据查询（自动更新工具数量）
- ✅ 分离 Server/Client 组件，解决 Ant Design 兼容性问题
- ✅ 导入初始数据：6 个分类，16 个工具
- ✅ 配置 Docker 镜像源，解决国内访问问题
- ✅ 建立 .env 备份机制
- ✅ 网站正式上线：https://aidaquanji.com
- ✅ 完成竞品分析：AICPB (AI产品榜)
- ✅ 制定短中长期改进方案，明确差异化策略

### 2026-01-07
- ⚠️ 遇到多个部署问题（详见"问题与解决方案"章节）
- 🔧 逐一排查和解决技术难题

---

## 🔗 相关链接

- **GitHub 仓库**: https://github.com/davidliuzhibo/ai-tools-directory
- **网站地址**: https://aidaquanji.com
- **Next.js 文档**: https://nextjs.org/docs
- **Prisma 文档**: https://www.prisma.io/docs
- **Docker 文档**: https://docs.docker.com
- **Ant Design**: https://ant.design

---

## 👥 联系方式

- **管理员邮箱**: davidliuzhibo@foxmail.com
- **服务器**: root@8.130.157.251

---

**文档结束**

> 💡 **提示**: 本文档会随着项目发展持续更新。如有任何疑问或需要补充，请及时更新此文档。
